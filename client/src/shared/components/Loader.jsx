/**
 * Loader component — reusable loading spinner with message.
 * TODO: Implement with MUI CircularProgress
 */
const Loader = ({ message = 'Loading...' }) => {
    return null;
};

export default Loader;
