/**
 * Navbar component — conditional rendering based on auth state.
 * TODO: Implement navigation with Login/Register (guest) and Dashboard/Logout (authenticated)
 */
const Navbar = () => {
    return null;
};

export default Navbar;
