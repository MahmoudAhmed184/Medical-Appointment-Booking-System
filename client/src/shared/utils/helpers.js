// TODO: Implement formatDate(date, format)
export const formatDate = (date, format) => { };

// TODO: Implement formatTime(time) — convert HH:mm to 12-hour
export const formatTime = (time) => { };

// TODO: Implement getStatusLabel(status)
export const getStatusLabel = (status) => status;

// TODO: Implement getStatusColor(status) — return MUI chip color
export const getStatusColor = (status) => 'default';
