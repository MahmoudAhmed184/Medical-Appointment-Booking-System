import { Outlet } from 'react-router-dom';

/**
 * AdminLayout — sidebar navigation for admin panel.
 * TODO: Implement with Navbar, Drawer (Dashboard, Users, Specialties, Appointments), content area
 */
const AdminLayout = () => {
    return <Outlet />;
};

export default AdminLayout;
