import { Outlet } from 'react-router-dom';

/**
 * PatientLayout — sidebar navigation for patient panel.
 * TODO: Implement with Navbar, Drawer (Dashboard, Find Doctors, My Appointments, Profile), content area
 */
const PatientLayout = () => {
    return <Outlet />;
};

export default PatientLayout;
