import { Outlet } from 'react-router-dom';

/**
 * DoctorLayout — sidebar navigation for doctor panel.
 * TODO: Implement with Navbar, Drawer (Dashboard, Availability, Appointments, Profile), content area
 */
const DoctorLayout = () => {
    return <Outlet />;
};

export default DoctorLayout;
